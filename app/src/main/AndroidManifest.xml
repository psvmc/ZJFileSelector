<?xml version="1.0" encoding="utf-8"?>
<manifest package="cn.psvmc.fileselectordemo"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name="cn.psvmc.fileselector.activity.FileHomeActivity" android:theme="@style/ZJAppTheme"/>
        <activity android:name="cn.psvmc.fileselector.activity.FileFolderActivity" android:theme="@style/ZJAppTheme"/>
        <activity android:name="cn.psvmc.fileselector.activity.FileMyActivity" android:theme="@style/ZJAppTheme"/>
    </application>

</manifest>